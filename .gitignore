# Go
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
coverage.html
coverage.out
vendor/

# Build output — server/bin/ is produced by build.ps1
server/bin/

# Regenerated from server/cmd/server/mga.ico (Windows .exe icon — run build.ps1 or go generate)
server/cmd/server/rsrc_windows_*.syso

# SQLite and local DBs
*.db
*.db-journal
*.db-wal
*.db-shm
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm

# Local config and secrets (track config.json.example, not config.json)
config.json
server/plugins/*/config.json

# OAuth tokens (Xbox, Epic, etc.)
tokens.json
server/plugins/*/tokens.json

# Integration config files with credentials
*-integration.json

# Temp / generated output
*.log
*.tmp
server/integrations_dump.json
server/scan_smb_result.json

# IDE and editors
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Node (if used)
node_modules/
.pnpm-store/
server/frontend/node_modules/
server/frontend/dist/

# Cursor / agent
.cursor/

# Local design drops (assets belong in docs/ and server/frontend/public/)
temp-resources/

# SuitCode
.suit/*
