# Go
*.exe
*.exe~
*.dll
*.so
*.dylib
/server/server
/server/server.exe
*.test
*.out
coverage.html
coverage.out
vendor/

# SQLite and local DBs
*.db
*.db-journal
*.db-wal
*.db-shm

# Local config and secrets (track config.json.example, not config.json)
config.json
server/config.json
server/bin/config.json
server/bin/plugins/*/config.json
server/plugins/*/config.json

# OAuth tokens (Xbox, Epic, etc.)
tokens.json
server/plugins/*/tokens.json
server/bin/plugins/*/tokens.json

# Integration config files with credentials
*-integration.json

# IDE and editors
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Node (if used)
node_modules/
.pnpm-store/

# Build and temp
/bin/
/tmp/
*.log
*.tmp

# Cursor / agent (optional; remove if you want to track plans)
.cursor/
