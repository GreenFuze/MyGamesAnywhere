{
  "plugin_id": "game-source-steam",
  "plugin_version": "1.4.1",
  "api_major": 1,
  "capabilities": ["source", "achievements"],
  "provides": ["source.games.list", "achievements.game.get", "plugin.check_config", "auth.oauth.callback", "auth.qr.begin", "auth.qr.poll"],
  "exec": "steam-source.exe",
  "default_timeout_ms": 120000,
  "max_concurrency": 1,
  "config": {
    "refresh_token": {
      "type": "string",
      "required": false,
      "x-secret": true,
      "x-auth-method": "qr",
      "description": "Sign in by scanning a QR code with the Steam mobile app. This is what MGA uses to read your library, and it is the only way to see Steam Family shared games. Your Steam password and Steam Guard code never reach MGA. Lasts for months and renews automatically."
    },
    "api_key": {
      "type": "string",
      "required": false,
      "x-secret": true,
      "x-help-url": "https://steamcommunity.com/dev/apikey",
      "description": "Only needed for achievements. Steam refuses a signed-in account for achievement data and accepts nothing but a key, so without one your games still appear and their achievements do not. Not used for your library."
    }
  }
}
