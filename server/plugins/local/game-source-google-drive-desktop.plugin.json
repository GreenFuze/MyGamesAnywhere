{
  "plugin_id": "game-source-google-drive-desktop",
  "plugin_version": "1.1.0",
  "api_major": 1,
  "capabilities": ["source"],
  "provides": [
    "source.filesystem.list",
    "source.filesystem.delete",
    "source.browse",
    "plugin.check_config"
  ],
  "exec": "local.exe",
  "default_timeout_ms": 300000,
  "max_concurrency": 1,
  "config": {
    "base_path": {
      "type": "string",
      "required": true,
      "description": "The synced Google Drive folder on this server. MGA offers the accounts it finds; pick one, or type a path if your Drive is mounted somewhere else."
    },
    "include_paths": {
      "type": "array",
      "required": true,
      "description": "Folders inside your Drive to scan, such as Games. Leave a path empty to scan the whole Drive.",
      "items": {
        "type": "object",
        "properties": {
          "path": { "type": "string", "required": true },
          "recursive": { "type": "boolean" },
          "exclude_paths": {
            "type": "array",
            "description": "Folders inside this include path to skip recursively.",
            "items": { "type": "string" }
          }
        }
      }
    }
  }
}
